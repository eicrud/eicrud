{
    "name": "@eicrud/cli",
    "version": "0.0.10",
    "description": "cli for @eicrud framework",
    "author": "acrosett",
    "license": "UNLICENSED",
    "type": "module",
    "files": [
        "dist"
    ],
    "bin": {
        "eicrud": "./dist/commands/index.js"
    },
    "scripts": {
        "generate": "./commands/index",
        "compile": "rimraf dist && tsc && cpx templates/** dist/templates",
        "publish:package": "npm run compile && npm publish"
    },
    "devDependencies": {
        "cpx": "^1.5.0",
        "rimraf": "^5.0.5"
    },
    "dependencies": {
        "commander": "^12.0.0"
    }
}
